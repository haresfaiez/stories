apply plugin: 'java'
apply plugin: 'idea'

apply from  : 'functionaltest.gradle'

ext {
    junit_version = '4.12'
}

dependencies {
    testCompile group:'junit', name:'junit', version: junit_version
}

repositories {
    mavenCentral()
}
